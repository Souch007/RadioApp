<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="radioLists"
            type="com.netcast.radio.ui.radio.data.temp.RadioLists" />

        <variable
            name="type"
            type="String" />

        <variable
            name="listener"
            type="com.netcast.radio.ui.radio.adapter.OnClickListnerRadio" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/_4sdp"
        android:elevation="@dimen/_8sdp"
        android:onClick="@{() -> listener.onRadioClicked(radioLists,type)}"
        app:cardBackgroundColor="#151523"
        app:cardCornerRadius="@dimen/_5sdp"
        app:cardElevation="@dimen/_3sdp">

        <LinearLayout
            android:layout_width="@dimen/_80sdp"
            android:layout_height="@dimen/_110sdp"
            android:orientation="vertical"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <ImageView
                    android:id="@+id/imageView"
                    android:layout_width="@dimen/_80sdp"
                    android:layout_height="@dimen/_80sdp"
                    android:src="@drawable/logo"
                    app:setImage="@{radioLists.favicon}" />


            </RelativeLayout>


            <TextView
                android:id="@+id/textView"
                style="@style/textTitle"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:ellipsize="end"
                android:gravity="center"
                android:lines="2"
                android:maxLines="2"
                android:padding="@dimen/_2sdp"
                android:text="@{radioLists.name}"
                android:textSize="@dimen/_10sdp"
                android:textStyle="normal"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHorizontal_bias="0.5"
                app:layout_constraintStart_toStartOf="parent"
                tools:text="asdfasdfasd" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="@dimen/_80sdp"
            android:layout_height="@dimen/_110sdp"

            android:background="#C9979797"
            android:gravity="center"
            android:visibility='@{radioLists.isBlocked ? View.VISIBLE : View.GONE}'>

            <androidx.appcompat.widget.AppCompatImageView
                style="@style/textsubheading"
                android:layout_width="@dimen/_30sdp"
                android:layout_height="@dimen/_30sdp"
                android:layout_gravity="center"
                android:src="@drawable/ic_closecircle"
                app:tint="@color/Red" />

        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>